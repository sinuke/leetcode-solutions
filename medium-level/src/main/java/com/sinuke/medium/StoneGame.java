package com.sinuke.medium;

public class StoneGame {

    public boolean stoneGame(int[] piles) {
        // int acnt = 0, bcnt = 0;
        // int l = 0, r = piles.length - 1, step = 1;
        // while (r >= l) {
        //     if (piles[l] > piles[r]) {
        //         if (step % 2 == 0) bcnt = piles[l++];
        //         else acnt = piles[l++];
        //     } else {
        //         if (step % 2 == 0) bcnt = piles[r--];
        //         else acnt = piles[r--];
        //     }
        //     step++;
        // }
        // return acnt > bcnt;
        return true;
    }

}
